import React ,{useState} from 'react'

export default function CreateTeams(props){
    // const [createdGame, setCreatedGame] = useState(props);
    // console.log(props.createdGame.title);


    return(
        <p>Teams! for { props.createdGame.title } </p>
        
    )
}